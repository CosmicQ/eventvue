#!/usr/bin/envbash

which aws
if [ $? -ne 0 ]; then
    echo "AWS CLI is not installed or not in the PATH"
    exit 1
fi

aws cloudformation create-stack \
  --stack-name EventVueTest \
  --template-body file://test_alarm.yml \
  --capabilities CAPABILITY_IAM